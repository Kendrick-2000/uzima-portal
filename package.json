{
  "name": "uzima-portal",
  "version": "2.4.1",
  "description": "Uzima University Academic Portal",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "gh-pages": "^6.1.0"
  }
}